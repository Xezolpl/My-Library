<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.xezolpl.mylibrary">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".activities.AddBookActivity"
            android:theme="@style/AlertDialog.AppCompat.Light.Dialog.NoActionBar" />

        <activity
            android:name=".activities.AddQuoteActivity"
            android:theme="@style/AlertDialog.AppCompat.Light.Dialog.NoActionBar" />

        <activity
            android:name=".activities.AddCategoryActivity"
            android:theme="@style/AlertDialog.AppCompat.Light.Dialog.NoActionBar" />

        <activity
            android:name=".activities.OpenedBookActivity"
            android:parentActivityName=".activities.AllBooksActivity" />

        <activity android:name=".activities.AllBooksActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>
    </application>
</manifest>